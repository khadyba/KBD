/** @type {import('next').NextConfig} */
const nextConfig = {

  crossOrigin: 'anonymous',
  images: {
    domains: ['www.kiwop.com'],  // Remplace par le domaine de tes images
  },
};

export default nextConfig;
